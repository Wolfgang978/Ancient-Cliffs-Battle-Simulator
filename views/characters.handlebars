<section class="container">
    <h3 class="gallery-name">{{gallery.name}}</h3>
    <section class="flex-row justify-center">
        {{#each characterData as |charcaters|}}
        {{> characters-details}}
        {{/each}}
    </section>
</section>